---
- name: NTP
  hosts: all
  collections:
    - arubanetworks.aoscx
  tasks:
    - name: NTP
      aoscx_config:
        lines:
          - ntp enable
          - ntp server ntp.wacofhc.org
          - clock timezone america/chicago
